---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unmanic-config-pvc
  namespace: unmanic-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
