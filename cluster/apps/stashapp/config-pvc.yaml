---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stash-pvc
  namespace: stash-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
