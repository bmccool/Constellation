---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-transmission-config-pvc
  namespace: sonarr-transmission-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
